mode: standalone

rootUser: minioadmin
rootPassword: minioadmin123

persistence:
  enabled: true
  size: 10Gi
  storageClass: standard   # verify with kubectl get storageclass

resources:
  requests:
    memory: 512Mi
